# Skye 双臂遥操 · Orin 高频链路优化路线（可执行）

**适用仓库：** [Skye_ws](https://github.com/Cgxiaoxin/Skye_ws)（`factr_apex_adapter.py` + Apex Orin）  
**目标：** 在不重写整套遥操的前提下，提高有效控制频率、降低 Orin CPU、简化启动。  
**原则：** 先改 QoS / 发布模型（低风险），再评估步长与进程合并，最后再上 C++ 或直连。  
**文档位置：** `遥操高频优化路线.md`

---

## 0. 先弄懂：QoS `KeepLast(1)` + `BEST_EFFORT`

遥操控制流是「最新关节角最重要」，**旧帧没有价值**。

### 0.1 History = Keep Last，Depth = 1

| 概念 | 含义 |
|------|------|
| Keep Last | 订阅端队列只保留最近 N 条 |
| Depth = 1 | N=1，**只留最新一帧**；新来的覆盖旧的 |

**为什么适合遥操：**  
500 Hz 时若 Depth=10，积压 10 帧旧命令再执行 → 延迟、抖动、CPU 忙着拷贝/排队。Depth=1 时永远跟最新目标，旧命令直接丢。

### 0.2 Reliability = BEST_EFFORT（尽力而为）

| 模式 | 行为 | 遥操影响 |
|------|------|----------|
| RELIABLE | 丢包会重传、等确认 | 高频下 DDS 重传占 CPU，旧包更晚到 |
| BEST_EFFORT | 丢了就算了，继续发新的 | 偶发丢 1 帧无妨；下一帧马上到 |

**命令 / 反馈**都建议 BEST_EFFORT + KeepLast(1)。  
**例外：** `/teleop/state`、`set_enabled` 这类低频状态/服务，可继续 RELIABLE（或 Transient Local），不要和 500 Hz 控制流混用同一套激进 QoS。

### 0.3 和当前代码的差距

`Skye_ws/marvin_ws/factr_apex_adapter.py` 现状大致是：

- `qos_depth` 默认 **10**
- 控制订阅常用 **reliable**
- 命令发布常用 **best_effort**（不对称）
- 命令靠 **`--publish-rate` 定时器**（默认 50，文档曾拉到 100/250/500）重发

优化方向：命令收发两侧统一 `KeepLast(1)+BEST_EFFORT`，并尽量改为「收到就发」。

---

## 1. 现状链路（改之前先对齐）

```text
开发机 Docker: factr_teleop_*
  发 /left|right_joint_control     (JointState, ~高 Hz)
  收 /left|right_joint_state       (JointState, sync 用)

网线 / 同一 ROS_DOMAIN_ID

Orin: factr_apex_adapter_{left,right}.py   ← Python，CPU 热点
  收 /left|right_joint_control
  收 /info/joint_feedback → 转发 /left|right_joint_state
  定时器 publish-rate → /control/joint_cmd_A|B (JointcmdArm)
  可选夹爪 → /control/gripperValueL|R

Orin: Apex 本体 + 夹爪节点
```

适配器不只是改 topic 名，还做：**类型转换、映射、限步、使能门控、夹爪**。  
**Remap 不能单独替代它**（类型不同：`JointState` vs `JointcmdArm`）。

### Remap 能做什么

```bash
# 仅当两边都是同一 msg 类型时有效
ros2 run <pkg> <node> --ros-args \
  -r /internal_name:=/graph_name
```

含义：节点内部仍用旧名，DDS 图上对外变成新名。  
**不能**完成类型转换、安全门控、关节符号映射。

---

## 2. 总体路线（三阶段）

| 阶段 | 内容 | 风险 | 预期收益 |
|------|------|------|----------|
| **P0 短期** | QoS + 回调发布 + 步长/频率匹配 +（可选）单进程双臂 | 低 | Orin CPU↓，有效频率↑，抖动↓ |
| **P1 中期** | 薄 C++ 适配器，或 Apex 直接订 JointState | 中 | Orin 上 Python 热点消失 |
| **P2** | Remap 简化名字 + 全链路统一 QoS；启动脚本合并 | 低 | 步骤变少，维护简单 |

建议：**先做完 P0 并测一周，再决定是否上 P1。**

---

## 3. P0 短期（仍用现 Python 适配器）— 可执行清单

**改动仓库：** `/data/coding/tianji/Skye_ws`（或 Orin 上 `/home/nvidia/factr_apex_adapter.py`）

### 3.1 改 QoS（命令 + 反馈）

**文件：** `marvin_ws/factr_apex_adapter.py`

**目标配置：**

```python
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy, DurabilityPolicy

control_qos = QoSProfile(
    history=HistoryPolicy.KEEP_LAST,
    depth=1,
    reliability=ReliabilityPolicy.BEST_EFFORT,
    durability=DurabilityPolicy.VOLATILE,
)
# 用于：
# - sub /left|right_joint_control
# - pub /control/joint_cmd_A|B
# - sub /info/joint_feedback（若高频）
# - pub /left|right_joint_state（若高频）
```

同步建议：

- 默认 `--qos-depth` 从 `10` 改为 `1`
- 控制订阅从 `reliable_qos` 改为与命令发布一致的 `control_qos`（BEST_EFFORT + depth=1）

**不要改成 BEST_EFFORT 的：**

- `/teleop/state`（可 Transient Local + RELIABLE）
- `~/set_enabled`（Service，与 topic QoS 无关）

**验收：**

```bash
# Orin
ros2 topic info -v /left_joint_control
ros2 topic info -v /control/joint_cmd_A
# 确认 Publisher/Subscription 的 QoS：depth=1, BEST_EFFORT
```

**注意：** factr 侧若仍是 RELIABLE+depth 较大，可能出现「能发现 topic 但收不到」的 QoS 不兼容。  
处理：小臂 Docker 启动时也给相关话题对齐 QoS，或两端都改成 BEST_EFFORT depth=1。以实机 `topic hz` 为准。

---

### 3.2 控制改为「回调触发发布」，去掉固定 timer×N 空转

**现状问题：**

```text
on_control() 只更新 self.target
on_timer() 每 1/publish_rate 秒才真正发 JointcmdArm
```

当 `publish-rate=500` 时：即使小臂暂时没动，timer 仍每 2ms 跑一轮 Python（限步、跟随误差、publish）→ Orin CPU 虚高。

**目标模型：**

```text
on_control(msg):
  映射 + 校验 → target
  若 enabled 且 TELEOP 且反馈有效:
      limit_step → 立刻 publish JointcmdArm
      （可选）同步发夹爪

on_timer: 仅做低频看门狗（如 20~50 Hz）
  - control/feedback 超时 → auto_disable
  - 跟随误差过大 → auto_disable
  - 不再每拍无条件重发命令
    （或仅在「超过 X ms 无新命令」时发 hold 帧）
```

**落地步骤：**

1. 把 `on_timer` 里「构造 JointcmdArm 并 publish」抽成 `publish_command_once()`。
2. 在 `on_control` 末尾，满足使能条件时调用它。
3. Timer 降到 **20~50 Hz**，只做安全检查；`--publish-rate` 可改名为 `--watchdog-hz`，或保留但语义变为看门狗频率。
4. 夹爪：可在 `on_gripper_control` 里事件驱动发布，或随关节命令一起发。

**验收：**

```bash
# 未使能 / IDLE：适配器 CPU 应明显下降（top/htop 看 python3 factr_apex_adapter）
# 使能 TELEOP 后：
ros2 topic hz /control/joint_cmd_A
# 期望：接近小臂 /left_joint_control 的 hz，而不是固定卡在旧 publish-rate
```

---

### 3.3 评估 `max_step` 与目标频率匹配

**公式（近似跟踪带宽）：**

```text
最大关节角速度 ≈ max_step_rad × 实际发布频率
```

| max_step_rad | @50 Hz | @200 Hz | @500 Hz |
|--------------|--------|---------|---------|
| 0.005 | 0.25 rad/s | 1.0 rad/s | 2.5 rad/s |
| 0.01 | 0.50 rad/s | 2.0 rad/s | 5.0 rad/s |
| 0.02 | 1.00 rad/s | 4.0 rad/s | 10 rad/s |

**操作：**

1. 先固定安全：`max_following_error`、`max_enable_error` 不变。
2. 在空旷工位，从当前 `max-step-rad` 起，**每次只乘 1.5~2**，测：
   - 快速甩臂是否跟手
   - 是否触发 `following error` auto_disable
   - 大臂是否冲击/异响
3. 记录最终参数到启动文档（左右可分开）。

**建议初始组合（实机再调）：**

```text
事件驱动发布 + max-step-rad 0.01~0.015
watchdog 50 Hz
QoS depth=1 BEST_EFFORT
```

---

### 3.4（可选）左右臂合并单进程

**现状：** Orin 上两个 `python3 factr_apex_adapter.py --side left/right` → 两个解释器、两套 GIL。

**做法：**

1. 新增入口或参数 `--sides left,right`（或 `both`）。
2. 一个 `MultiThreadedExecutor`，两个 arm 上下文（或一个 Node 内两套 pub/sub）。
3. 启动文档改成一条命令；`set_enabled` 可保留左右两个 service，或一个 service 带 side。

**验收：** `pgrep -af factr_apex` 只见一个主进程；CPU 总和应低于「两进程之和」。

**优先级：** 低于 3.1 / 3.2；可 P0 末尾再做。

---

### 3.5 P0 启动参数模板（改完后）

```bash
# Orin 示例（事件驱动后 publish-rate 仅作 watchdog）
python3 -u /home/nvidia/factr_apex_adapter.py \
  --side left --execute \
  --enable-gripper-bridge \
  --qos-depth 1 \
  --publish-rate 50 \
  --max-step-rad 0.01 \
  ...
```

同步更新：`Skye_ws/Docs/遥操启动文档.md` 中的左右启动命令与验收表。

---

### 3.6 P0 测试清单（必须打勾再进 P1）

- [ ] `ros2 topic info -v`：控制/命令 QoS 为 depth=1、BEST_EFFORT
- [ ] IDLE 时适配器 CPU < 改前明显一档
- [ ] TELEOP 时 `hz(/control/joint_cmd_*)` ≈ `hz(/_*_joint_control)`
- [ ] sync → teleop → `set_enabled true` 流程仍安全（无初始大跳）
- [ ] 超时 / 跟随误差仍会 auto_disable
- [ ] 夹爪开合正常
- [ ] 连续遥操 10~15 min 无异常升温/失控

---

## 4. P1 中期（收益最大）— 二选一

### 方案 A：薄 C++ 适配器（推荐，改动面可控）

**目标：** 语义与现 Python 一致，热路径离开 GIL。

**包建议：** `factr_apex_adapter_cpp`（ament_cmake）

**必须移植的行为：**

1. `Jointfeedback` → `JointState`（7 轴切片）
2. `JointState` → `JointcmdArm`（indices/signs/offsets）
3. `limit_step`、跟随误差、超时、`set_enabled`、TELEOP 门控
4. 可选夹爪 Float32
5. QoS：KeepLast(1)+BEST_EFFORT；**回调发布**

**不做：** 复杂业务堆在 Python 里慢慢迁；先 1:1 行为对齐。

**验收：**

```bash
# 同输入下，命令 positions 与 Python 版最大误差 < 1e-6（单测）
# Orin: 同高频输入时 C++ 节点 CPU 目标个位数 %
```

**切换：** 启动文档把 `python3 factr_apex_adapter.py` 换成 `ros2 run ...`；保留 Python 一周作回滚。

---

### 方案 B：Apex 直接吃 `JointState`（Remap 才真正「直连」）

**前提：** 能改 Apex / `marvin_robot_node`（或加一层原生订阅）。

**步骤：**

1. 大臂增加订阅：`sensor_msgs/JointState`，字段 `position[7]`，单位 rad。
2. 映射（signs/order）放进大臂参数或小臂侧。
3. 安全：enable 可仍由服务控制「是否消费该 topic」。
4. 小臂或大臂用 Remap 对齐名字，例如：

```bash
# 仅当大臂已改为 JointState 且约定好 topic 名
-r /joint_control:=/left_arm_cmd
# 或大臂侧：
-r /arm_cmd:=/left_joint_control
```

5. 适配器缩成：仅反馈 `Jointfeedback→JointState`（若大臂反馈类型仍不同），或完全删除。

**Remap 在此阶段才成为「主对齐手段」。**

---

## 5. Remap 与 QoS 的关系（运维认知）

| 手段 | 解决什么 | 不解决什么 |
|------|----------|------------|
| Remap | 同类型下的 topic 名 | 类型转换、安全、映射 |
| QoS depth=1 BEST_EFFORT | 队列堆积、重传占 CPU、跟最新帧 | 类型不一致 |
| 回调发布 | timer 空转 CPU | 跨机序列化 |
| C++ / 同机 Component | GIL、同机零拷贝 | 跨机仍须 DDS 序列化 |

当前是 **开发机 ↔ Orin 两台机器** → **不要做 intra-process Component 零拷贝预期**。

---

## 6. 建议排期

| 天 | 任务 |
|----|------|
| D1 | 改 QoS + 实机确认两端互通；记录 hz/CPU 基线 |
| D2 | 回调发布 + timer 降频；对比 CPU 与 hz |
| D3 | 调 max_step；更新启动文档参数 |
| D4 | （可选）单进程双臂 |
| D5+ | 立项 P1A（C++ 薄适配器）或 P1B（Apex 直接 JointState） |

---

## 7. 回滚

- 保留旧启动命令与旧 `factr_apex_adapter.py` 备份路径。
- 异常时：`set_enabled false` → 停适配器 → 恢复旧参数（RELIABLE / depth=10 / timer 模式）→ 再查 log。

```bash
ros2 service call /factr_apex_adapter_left/set_enabled  std_srvs/srv/SetBool "{data: false}"
ros2 service call /factr_apex_adapter_right/set_enabled std_srvs/srv/SetBool "{data: false}"
pkill -f factr_apex_adapter.py || true
```

---

## 8. 成功标准

1. Orin 上适配器相关 CPU：**TELEOP 时明显低于改前**（建议目标减半或进个位数，视负载而定）。
2. `/control/joint_cmd_*` 有效频率：**稳定跟随小臂控制流**（例如 ≥200 Hz 且延迟可接受）。
3. 安全门控与夹爪行为：**与现网一致**。
4. 启动步骤：**不增加**；中期目标是减少适配器相关手工步骤。

---

## 9. 相关代码与文档索引

| 路径 | 说明 |
|------|------|
| `/data/coding/tianji/Skye_ws/marvin_ws/factr_apex_adapter.py` | 现网 Python 适配器（优化主目标） |
| `/data/coding/tianji/Skye_ws/Docs/遥操启动文档.md` | 日常启动步骤 |
| `/data/coding/tianji/Skye_ws/Docs/抖动优化问题.md` | 既有频率/抖动记录 |
| `/data/coding/tianji/Skye_ws/Docs/遥操作文档v1.md` | 架构与适配器说明 |
| 本仓库 `marvin_ws/gento_ros2_ws/` | Gento 本机 SDK 直连路径（两端已是 JointState，无 Apex 适配器） |

**说明：** Gento 直连与 Apex/`JointcmdArm` 是两条架构。本路线针对 **Skye_ws Orin 适配器** 优化；不要与 Gento bridge-less 路径混改。
